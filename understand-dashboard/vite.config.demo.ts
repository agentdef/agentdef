// Intentionally emptied (not deleted -- this mount does not reliably support
// deleting or renaming already-written files). This standalone, non-workspace
// copy of the dashboard has no ../core/dist to alias against, and the
// project's build/dev scripts no longer reference this file
// ("build": "vite build", "dev": "vite" -- both use vite.config.ts).
export {};
